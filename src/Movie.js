import React from 'react';

const Movie = () => {
     
    return(
        <div>
            {/* {movies.map(movie => (
                <li>{movie.name}</li>
            ))} */}
        </div>
    );
}

export default Movie